<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Status do Login</title>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      display: flex; 
      justify-content: center; 
      align-items: center; 
      height: 100vh; 
      background-color: #f2f2f2;
    }
    div {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.2);
      text-align: center;
    }
    a { 
      color: #007bff; 
      text-decoration: none; 
    }
    a:hover { 
      text-decoration: underline; 
    }
  </style>
</head>
<body>
  <div>
    <h1><%= status %></h1>
    <% if (usuario) { %>
      <p>Bem-vindo(a), <%= usuario %>!</p>
    <% } else { %>
      <p>Por favor, tente novamente.</p>
    <% } %>
    <a href="/atividade8/login.html">Voltar ao login</a>
  </div>
</body>
</html>
